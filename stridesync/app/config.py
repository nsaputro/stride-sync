# Settings loaded from /data/options.json (via bashio::config in each service's run script,
# passed through as env vars) — see PROJECT_PLAN.md §1, HA add-on configuration table.
#
# TODO (v0.1): garmin_username, garmin_password, sync_interval_hours, mcp_port, log_level
