# Garmin Connect sync — see PROJECT_PLAN.md §1
#
# Switched from garmy to python-garminconnect after three successive fixes on top of garmy
# (User-Agent, TLS-fingerprint impersonation, human-like login timing — see CHANGELOG.md) all
# failed to get past Garmin's Cloudflare bot challenge on the SSO login. python-garminconnect
# already implements a 5-strategy cascading login chain (mobile/widget/portal x curl_cffi TLS
# impersonation, falling through non-credential/non-MFA failures to the next strategy) plus its
# own anti-bot-detection timing delays, actively maintained against Garmin's changes — see
# app/sync/garmin_client.py's module docstring.
garminconnect
requests  # used directly in app/sync/garmin_client.py to catch transport-level auth failures

# MCP server — fastmcp serves Streamable HTTP natively (transport="http"), no mcp-proxy needed
# inside the add-on; see app/mcp/server.py module docstring for why garmy-mcp's bundled server
# wasn't reused as-is.
fastmcp
